//cria classe tweets
class Followers {
    // cria método para buscar os followers no json
    buscaSeguidores() {
        // retorna as informações no follow.json
        return fetch("./follow.json")
    }
}